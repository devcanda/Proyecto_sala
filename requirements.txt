# --- Backend core ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Base de datos ---
# SQLite viene incluido en la librería estándar de Python (modo Standalone).
psycopg2-binary==2.9.9   # Driver PostgreSQL (modo Red LAN)

# --- Hardware (impresoras térmicas / cajón monedero) ---
python-escpos==3.1

# --- Imagenes de producto (redimensionado automatico) ---
Pillow==10.4.0

# --- Utilidades ---
python-dotenv==1.0.1

# --- IA / Analítica (fase posterior, dejar preparado) ---
# pandas
# scikit-learn
